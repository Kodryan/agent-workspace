# Agent Workspace: Feed Demo

Synthetic feed demo. Do not search other folders under `Developer`.

## Feed requests

For “лента”, “выдача”, “feed”, “карточка”, card analytics, `ios-feed-app`, or `analytics-contracts`:

1. Read `Repositories.md` for exact repository paths and responsibilities.
2. Read the relevant repository's `AGENTS.md` before changing code.
3. Use `Sources.md` to find the authoritative source for the needed detail.
4. Verify the relevant contract, implementation, and tests.

Load or create project and task notes only when needed.

## Rules

- Verify changing details in source code; report contradictions.
- Run the target package tests after edits.
- Change analytics contracts only when the task explicitly changes the schema.
